#pragma once

#include <string>

double SlopeLimiter(const double &a0,
                    const double &a1,
                    const double &a2,
                    const double &deltax,
                    const std::string &kind);
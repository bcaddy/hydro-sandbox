name: C/C++ CI

on:
  push:
    branches: [ testing-prototype ]
  pull_request:
    branches: [ testing-prototype ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: cd-test
      run: cd tests
    - name: make
      run: make
    - name: run-tests
      run: ./Tests.exe
